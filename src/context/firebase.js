import { createContext } from "react";

// context provides us a bunch of functions from firebase that allow us to insert information
const FirebaseContext = createContext(null);

export default FirebaseContext;